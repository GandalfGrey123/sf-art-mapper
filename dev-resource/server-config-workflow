
amazon linux2 distribution

SERVER CONFIG - HTTPS 

open inbound rules in security wizard
https 443 anywhere
https 80 anywhere

update then install httpd and dependencies
sudo yum update -y
sudo yum install -y httpd

change apache permissions
sudo usermod -a -G apache ec2-user
exit
ssh .. back into server

give apache permissions to public www folder
sudo chown -R ec2-user:apache /var/www
sudo chmod 2775 /var/www && find /var/www -type d -exec sudo chmod 2775 {} \;
find /var/www -type f -exec sudo chmod 0664 {} \;


sudo yum update -y
sudo yum install -y mod_ssl <-- generates the config file and locahost.crt

cd /etc/pki/tls/certs
sudo ./make-dummy-cert localhost.crt

sudo cp localhost.crt /etc/pki/tls/private/localhost.key <--- before running the server we need to add the pvt key 
sudo systemctl start httpd 



SERVER CONFIG - DEV ENVIRONMENT


install nodejs
curl -sL https://rpm.nodesource.com/setup_10.x | sudo -E bash -
sudo yum install -y nodejs


install git
sudo yum install git
